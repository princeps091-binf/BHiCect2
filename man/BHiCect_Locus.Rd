% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall_bhicect.R
\name{BHiCect_Locus}
\alias{BHiCect_Locus}
\title{Targeted Multi-Resolution Clustering for a Specific Locus or TAD}
\usage{
BHiCect_Locus(MresFile, chrom, tad_start, tad_end, start_res, threshold = 0.5)
}
\arguments{
\item{MresFile}{An mcool file object handle initialized via \code{hictkR}.}

\item{chrom}{Character. The target chromosome (e.g., \code{"chr8"}).}

\item{tad_start}{Integer. The starting base-pair coordinate of the target interval.}

\item{tad_end}{Integer. The ending base-pair coordinate of the target interval.}

\item{start_res}{Integer. The resolution used to initialize
the root level analysis (e.g., \code{25000}).}

\item{threshold}{Numeric. Spectral split termination sensitivity threshold.
Defaults to \code{0.5}.}
}
\value{
A named \code{list} containing two structural elements:
\describe{
\item{\code{nodes}}{A flattened tibble containing tracking data for all identified
cluster nodes (including resolution level, depth, parent assignments, and
the raw bin ID coordinates).}
\item{\code{edges}}{A data frame mapping the hierarchical parent-to-child relationships
generated by the recursive bisection cuts.}
}
}
\description{
Isolates a specific genomic interval (e.g., a pre-called TAD or gene locus)
at a specified root resolution and performs BHiCect2
top-down recursive spectral decomposition to identify nested sub-structures.
}
\examples{
\dontrun{
# Execute targeted micro-decomposition on the MYC locus in HCT116
myc_tree <- BHiCect_Locus(
  MresFile  = hct116_mcool,
  chrom     = "chr8",
  tad_start = 127235000,
  tad_end   = 128243000,
  start_res = 25000,
  threshold = 0.45
)
}
}
