% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall_bhicect.R
\name{as_granges_list}
\alias{as_granges_list}
\title{Convert Cluster Summary Table to a Grouped GRangesList}
\usage{
as_granges_list(summary_tbl, current_MresFile, seqname = "chrUnknown")
}
\arguments{
\item{summary_tbl}{A data.frame or tibble. Must contain the columns:
\code{id} (character cluster path), \code{ids} (list column of numeric bin starts),
\code{res_level} (integer index), and \code{depth} (integer).}

\item{current_MresFile}{A list or object tracking the resolution vector
(e.g., \code{current_MresFile$resolutions}).}

\item{seqname}{Character. The chromosome name (e.g., "chr20").}
}
\value{
A \code{GenomicRanges::GRangesList} object grouped by unique cluster node IDs.
}
\description{
Takes a cluster summary table containing nested bin IDs and maps them into
a structured \code{GenomicRanges::GRangesList} object, grouping non-contiguous
interval chunks together under their unique parent cluster node IDs.
}
