% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/waterfall_bhicect.R
\name{fetch_nested_locations}
\alias{fetch_nested_locations}
\title{Fetch Nested High-Resolution Locations}
\usage{
fetch_nested_locations(chrom, ids, res_level, high_res_level, MresFile)
}
\arguments{
\item{chrom}{A character string specifying the chromosome (e.g., "chr1").}

\item{ids}{A numeric vector of starting positions (bin IDs) at the current resolution.}

\item{res_level}{Integer. The current resolution index (1 being the lowest/coarsest).}

\item{high_res_level}{Integer. The target higher resolution index.}

\item{MresFile}{A list or object containing multi-resolution file metadata,
including \code{path}, \code{resolutions}, and \code{chromosomes} info.}
}
\value{
A numeric vector of starting positions representing the high-resolution
bins nested within the input locations.
}
\description{
This function performs a recursive-style lookup to find high-resolution
genomic bins that are contained within (nested) a set of lower-resolution bins.
It is designed to work with multi-resolution files (e.g., .mcool) via the
\code{hictkR} package.
}
\details{
The function uses \code{GenomicRanges::countOverlaps} to identify which high-resolution
bins physically intersect with the range defined by the current resolution IDs.
If the \code{res_level} exceeds the available resolutions in \code{MresFile},
the original \code{ids} are returned.
}
\examples{
\dontrun{
# Assuming mres_metadata contains the path to an .mcool file
high_res_bins <- fetch_nested_locations(
  chrom = "chr1", 
  ids = c(1000000, 2000000), 
  res_level = 1, 
  high_res_level = 2, 
  MresFile = mres_metadata
)
}
}
