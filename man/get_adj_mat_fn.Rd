% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BHiCect.R, R/waterfall_bhicect.R
\name{get_adj_mat_fn}
\alias{get_adj_mat_fn}
\title{Produce adjacency matrix from tibble}
\usage{
get_adj_mat_fn(g_chr1)

get_adj_mat_fn(g_chr1)
}
\arguments{
\item{g_chr1}{An \code{igraph} object, typically representing a chromosomal
interaction network.}
}
\value{
The square and symmetric Adjacency matrix from the input graph

A weighted \code{dgCMatrix} (sparse matrix format) with zero
diagonals and no zero-sum columns/rows.
}
\description{
This function takes a chromosomal network (igraph object) and converts it into
a weighted adjacency matrix. It ensures the matrix has no self-loops by
zeroing the diagonal and removes any "orphan" nodes (nodes with no connections).
}
\examples{
\dontrun{
# Assuming g is a pre-existing igraph object
adj_matrix <- get_adj_mat_fn(g)
}
}
